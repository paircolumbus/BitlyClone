<%= form_for(@link) do |f| %>
  <% if @link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@link.errors.count, "error") %> prohibited this link from being saved:</h2>

      <ul>
      <% @link.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :old_url %><br>
    <%= f.text_field :old_url, id: :link_old %> <!-- I do not believe the id: works the way I want it to. -->
  </div>
  <div class="field">
    <%= f.label :new_url %><br>
    <%= f.text_field :new_url, id: :link_new %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
