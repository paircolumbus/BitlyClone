<p id="notice"><%= notice %></p>

<p>
  <strong>Destination:</strong>
  <%= @url.destination %>
</p>

<p>
  <strong>Shortenened URL:</strong>
  <%= "#{request.protocol}#{request.host_with_port}/#{@url.shortcode}" %>
</p>

<p>
  <strong>Clicks:</strong>
  <%= @url.click_count %>
</p>

<%= link_to 'Edit', edit_url_path(@url) %> |
<%= link_to 'Back', urls_path %>
